import 'package:flutter/material.dart';

class ResearchBanner extends StatelessWidget {
  const ResearchBanner({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
